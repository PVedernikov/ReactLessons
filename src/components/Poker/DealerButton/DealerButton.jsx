import React from 'react';
import classes from './DealerButton.module.css';

const DealerButton = (props) => {
    return (
        <div className={classes.dealerButton}>
            DEALER
        </div>
    );
};

export default DealerButton;